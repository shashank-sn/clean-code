interface:
  display_name: "Clean Verify"
  short_description: "Run approved checks and normalize evidence"
  default_prompt: "Use $clean-verify to run the approved checks against the final revision."
