# forwarding flags

The command line accepts a comma-delimited `--forward` value. Each item is
passed as one argument to the child process, in order; surrounding whitespace
around an item is ignored. An empty value produces no forwarded arguments.
