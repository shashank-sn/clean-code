# cache refresh

Refresh should preserve the caller's authorization and return the same visible
record. The packet intentionally omits the implementation and caller context;
respond that a reliable review is blocked by missing evidence.
