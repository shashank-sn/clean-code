# rule format

Rules are separated by semicolons. A backslash escapes the following
separator, so `name=alpha\\;beta;enabled=true` represents two rules and keeps
the escaped semicolon in the first value. Empty rules are ignored.
